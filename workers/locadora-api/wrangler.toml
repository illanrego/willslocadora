name = "locadora-api"
main = "src/index.mjs"
compatibility_date = "2026-07-22"

# Intentionally empty until the public GitHub Pages URL is known.
# Set the exact production and deliberate development origins before deployment.
[vars]
ALLOWED_ORIGINS = ""
